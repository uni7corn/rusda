# 构建关键文件必须使用 LF 行尾，否则在 Linux 构建机上：
#  - *.sh 会因 \r 报 "$'\r': command not found"
#  - *.patch 用 git apply 打到 LF 的官方 frida 源码上会因 CRLF 不匹配而失败
# （历史上部分文件被 Windows 提交意外转成了 CRLF）
*.sh      text eol=lf
*.patch   text eol=lf
*.py      text eol=lf
